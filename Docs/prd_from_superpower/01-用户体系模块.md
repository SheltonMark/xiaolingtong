# 用户体系模块

## 1. 用户类型定义

### 1.1 企业用户
| 字段 | 说明 |
|-----|------|
| 用户类型 | 企业用户 |
| 企业分类 | 工厂、工贸一体、电商、贸易公司、门店、其他单位（仅为标签，权限相同） |
| 认证状态 | 待认证、待审核、已认证 |

### 1.2 临工用户
| 字段 | 说明 |
|-----|------|
| 用户类型 | 临工用户 |
| 认证状态 | 待认证、待审核、已认证 |
| 特殊角色 | 可被指定为"临时管理员" |

---

## 2. 登录注册流程

### 2.1 登录方式
- **仅支持微信授权登录**
- 用户一键授权即可登录，无需手机号
- 手机号在资质认证时必填

### 2.2 注册流程

```
┌─────────────────────────────────────────────────────────────┐
│                      用户首次打开小程序                       │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
                    ┌─────────────┐
                    │ 微信授权登录 │
                    └──────┬──────┘
                           │
                           ▼
                    ┌─────────────┐
                    │ 选择用户类型 │
                    │ 企业 / 临工  │
                    └──────┬──────┘
                           │
              ┌────────────┴────────────┐
              │                         │
              ▼                         ▼
       ┌─────────────┐          ┌─────────────┐
       │  企业用户    │          │  临工用户    │
       │  进入企业端  │          │  进入临工端  │
       └─────────────┘          └─────────────┘
```

### 2.3 用户类型选择
- 首次登录必须选择用户类型
- **选择后不可更改**（如需更换，需联系客服处理）
- 不同用户类型看到的界面和功能完全不同

---

## 3. 企业用户认证

### 3.1 认证所需材料
| 字段 | 必填 | 说明 |
|-----|------|------|
| 企业名称 | ✅ | 与营业执照一致 |
| 统一社会信用代码 | ✅ | 18位代码 |
| 营业执照照片 | ✅ | 清晰可辨认 |
| 法人身份证正面 | ✅ | |
| 法人身份证反面 | ✅ | |
| 联系人姓名 | ✅ | |
| 联系人手机号 | ✅ | 需验证码验证 |
| 企业分类 | ✅ | 工厂/工贸一体/电商/贸易公司/门店/其他 |
| 一级行业分类 | ✅ | 日用百货/电子3C/服装服饰/模具加工/印刷包装/其他 |
| 二级品类标签 | ✅ | 根据一级分类多选 |
| 属性标签 | ✅ | 业务类型/产能标签/服务标签 |
| 联系微信（选填） | ❌ | |
| 企业地址 | ✅ | 省市区+详细地址 |

### 3.2 认证流程

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  填写资料   │ ──→ │  提交审核   │ ──→ │  平台审核   │ ──→ │  审核结果   │
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
                                               │
                                    ┌──────────┴──────────┐
                                    │                     │
                                    ▼                     ▼
                             ┌─────────────┐       ┌─────────────┐
                             │  审核通过   │       │  审核驳回   │
                             │  状态变更   │       │  推送原因   │
                             │  可发布信息 │       │  可重新提交 │
                             └─────────────┘       └─────────────┘
```

### 3.3 认证状态说明
| 状态 | 说明 | 可用功能 |
|-----|------|---------|
| 待认证 | 未提交认证资料 | 仅可浏览信息，不可发布，不可查看联系方式 |
| 待审核 | 已提交，等待平台审核 | 同上 |
| 已认证 | 审核通过 | 全部功能 |

---

## 4. 临工用户认证

### 4.1 认证所需材料
| 字段 | 必填 | 说明 |
|-----|------|------|
| 真实姓名 | ✅ | 与身份证一致 |
| 身份证号 | ✅ | 18位 |
| 身份证正面照片 | ✅ | |
| 身份证反面照片 | ✅ | |
| 手机号 | ✅ | 需验证码验证 |

### 4.2 自动识别信息
以下信息**不需要临工填写**，系统根据报名记录自动识别：
- 擅长工种
- 常在地区

### 4.3 认证状态说明
| 状态 | 说明 | 可用功能 |
|-----|------|---------|
| 待认证 | 未提交认证资料 | 可浏览用工信息，不可报名 |
| 待审核 | 已提交，等待平台审核 | 同上 |
| 已认证 | 审核通过 | 可报名应聘、可提现工资 |

---

## 5. 用户"我的"页面

### 5.1 企业用户"我的"页面

```
┌─────────────────────────────────────────┐
│              我的                        │
├─────────────────────────────────────────┤
│  头像  企业名称                          │
│  认证状态标识：待认证 / 待审核 / 已认证    │
├─────────────────────────────────────────┤
│  📋 我的发布                             │
│     - 采购需求 (N条)                     │
│     - 库存信息 (N条)                     │
│     - 代加工服务 (N条)                   │
│     - 用工招聘 (N条)                     │
├─────────────────────────────────────────┤
│  👁️ 查看机会                            │
│     剩余 N 次 | 购买更多                  │
├─────────────────────────────────────────┤
│  👑 会员中心                             │
│     当前：普通用户 / 月会员 / 年会员       │
├─────────────────────────────────────────┤
│  💰 用工管理（如有发布用工需求）           │
│     - 待支付订单                         │
│     - 进行中订单                         │
│     - 已完成订单                         │
├─────────────────────────────────────────┤
│  ⚙️ 设置                                │
│     - 企业资料                           │
│     - 联系客服                           │
│     - 关于我们                           │
└─────────────────────────────────────────┘
```

**UI设计要点：**
- 认证状态标识要醒目，使用不同颜色区分
  - 待认证：灰色
  - 待审核：橙色
  - 已认证：绿色 + 认证图标

### 5.2 临工用户"我的"页面

```
┌─────────────────────────────────────────┐
│              我的                        │
├─────────────────────────────────────────┤
│  头像  姓名                              │
│  认证状态标识：待认证 / 待审核 / 已认证    │
│  信用分：⭐ 95分                         │
├─────────────────────────────────────────┤
│  📊 接单统计                             │
│     累计接单：N 单                        │
│     累计收入：¥ XXXX                     │
├─────────────────────────────────────────┤
│  📋 我的报名                             │
│     - 待确认                             │
│     - 已入选                             │
│     - 已落选                             │
├─────────────────────────────────────────┤
│  💼 进行中的工作                         │
│     - 待出勤确认                         │
│     - 进行中                             │
├─────────────────────────────────────────┤
│  💰 我的钱包                             │
│     余额：¥ XXX.XX                       │
│     [提现]                               │
│     - 收入明细                           │
│     - 提现记录                           │
├─────────────────────────────────────────┤
│  ⚙️ 设置                                │
│     - 个人资料                           │
│     - 联系客服                           │
│     - 关于我们                           │
└─────────────────────────────────────────┘
```

---

## 6. 信用分体系

### 6.1 临工信用分
- **满分**：100分
- **初始分**：80分（后台可配置）
- **计算方式**：系统根据行为自动计算，无需人工评价

| 行为 | 分值变化 | 说明 |
|-----|---------|------|
| 完成订单 | +2分 | 正常完成一次用工 |
| 确认出勤但爽约 | -10分 | 点击确认后未到场 |
| 多次爽约 | -20分 | 累计3次以上 |
| 被投诉（核实） | -15分 | 工厂投诉且平台核实 |
| 旷工/早退 | -5分 | 临时管理员上报 |
| 虚报工时/计件 | -20分 | 严重违规 |

**信用分影响：**
| 分数区间 | 影响 |
|---------|------|
| 80-100 | 正常使用 |
| 60-79 | 报名时排序靠后 |
| 60以下 | 限制报名 |
| 30以下 | 封号 |

### 6.2 工厂信用分
- **满分**：100分
- **初始分**：80分（后台可配置）
- **计算方式**：系统根据行为自动计算

| 行为 | 分值变化 | 说明 |
|-----|---------|------|
| 按时支付工资 | +1分 | 结算后24小时内支付 |
| 延迟支付 | -5分 | 超过约定时间 |
| 拖欠工资 | -20分 | 超过7天未支付 |
| 被临工投诉（核实） | -10分 | |
| 线下交易 | -30分 | 严重违规 |
| 完成订单 | +2分 | 正常完成一次用工 |

---

## 7. 权限控制逻辑

### 7.1 页面可见性控制

```javascript
// 伪代码示例
function getVisibleModules(userType) {
  if (userType === 'enterprise') {
    return ['采购需求', '工厂库存', '代加工服务', '工厂名录'];
  } else if (userType === 'worker') {
    return ['用工招聘'];
  }
}
```

### 7.2 功能权限控制

```javascript
// 伪代码示例
function canPublish(user) {
  return user.type === 'enterprise' && user.certStatus === 'certified';
}

function canViewContact(user, infoType) {
  if (user.type === 'worker' && infoType === '用工招聘') {
    return true; // 临工免费查看用工联系方式
  }
  if (user.type === 'enterprise') {
    return user.isMember || user.viewChances > 0;
  }
  return false;
}
```

---

## 8. 接口清单

| 接口 | 方法 | 说明 |
|-----|------|------|
| /api/user/login | POST | 微信登录 |
| /api/user/register | POST | 注册（选择用户类型） |
| /api/user/profile | GET | 获取用户信息 |
| /api/user/profile | PUT | 更新用户信息 |
| /api/user/cert/enterprise | POST | 提交企业认证 |
| /api/user/cert/worker | POST | 提交临工认证 |
| /api/user/cert/status | GET | 获取认证状态 |
| /api/user/credit | GET | 获取信用分 |
